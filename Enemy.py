class Enemy:
        def __init__(self, name, lvl, hp, mp, atk):
                self.name = name
                self.lvl = lvl
                self.hp = hp
                self.mp = mp
                self.atk = atk